---
- hosts: jenkins-box
  gather_facts: false

  roles:
    - role: jenkins
      jenkins_users:
        - name: "Admin"
          password: "321"
          email: "no-reply@localhost"
      jenkins_plugins:
        - ghprb
        - slack
        - workflow-aggregator
        - pipeline-stage-view
        - github-organization-folder
        - modernstatus
